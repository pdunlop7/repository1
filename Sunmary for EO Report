Summary (EO Report) = 
VAR Pattern = "PTSNEW-" 
VAR SummaryText = 'PTS Raw'[Summary] 
VAR StartPos = SEARCH(Pattern, SummaryText, 1, -1) 

RETURN 
IF(
    StartPos = -1, 
    SummaryText, 
    TRIM(
        SUBSTITUTE(
            SummaryText, 
            MID(SummaryText, StartPos, 10),  -- Adjusted to remove "PTSNEW-#### "
            ""
        )
    )
)